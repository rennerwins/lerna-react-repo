{
  "name": "root",
  "private": true,
  "scripts": {
    "firstNative:ios": "lerna --scope='FirstNative' run ios --stream --",
    "firstNative:android": "lerna --scope='FirstNative' run android --stream --"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/react-native",
      "**/react-native/**"
    ]
  }
}
